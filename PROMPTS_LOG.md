# AI Prompts Log (supplementary to Section 7 of the report)

This file documents prompts used with Claude (via Claude Code) during this project's development, in more detail than the condensed summary in the report's "AI Prompts Used" section. It covers the sessions visible to the assistant that compiled this log; if teammates used Claude in sessions not captured here (e.g., separate frontend or data-collection sessions), those should be appended below before final submission.

## Coding

- "看看目前项目状况" (review current project state) — used to orient a new session on the existing codebase before further work.
- "写一个用llm 调用claude api的版本试试，因为好像效果很好，我实际测试一下体验感受" (build a version that actually calls the Claude API to test the LLM approach for real, since the earlier in-chat estimate looked promising) — led to the design and implementation of `backend/llm_eval/claude_classify.py`: a two-stage profile-then-batch-classify script using a forced tool call for structured output, evaluated against the identical train/test split as the hand-crafted model.
- Follow-up clarifying questions were asked back to the user before implementation: which model tier to use (Haiku 4.5 vs. Sonnet 5 vs. both), and which prompting strategy to use (summarize-then-classify vs. raw few-shot) — both were resolved by the user before code was written.

## Debugging

- Model output was checked for a truncated preference-profile generation (`stop_reason == "max_tokens"`); `max_tokens` was raised from 1024 to 2048 and the run repeated to confirm the truncation, not model capability, was responsible for a piece of an earlier result.
- Diagnosis of the exclamation-mark double-counting bug (`text.count("!") + text.count("!")` instead of `text.count("!") + text.count("！")`) and of the `is_bulk_header` cold-start recall failure on a job-hunting user's real inbox, both surfaced during evaluation against real collected data in an earlier session.

## Explanations

- "目前调用llm的算法具体是怎么做的？" (how exactly does the current LLM-calling algorithm work?) — requested and received a step-by-step walkthrough of the two-API-call profile/classify design, including why a forced tool call was used for structured output and why the design uses two calls total rather than one call per email.
- "我们那个手动提取特征的，又是什么方法？" (what is the hand-crafted feature-extraction method?) — requested and received a walkthrough of the 18-feature vector, the cold-start prior, and the online personalization update rule (`train_weights()`), and how it differs in kind from the LLM-based alternative.

## Research and evaluation design

- Discussion of how to generate priority labels for the unlabeled Enron corpus (folder-based heuristic), and why a custom-folders-as-Keep variant was tried and rejected based on held-out accuracy.
- A direct request to actually call a real LLM API (rather than continue relying on an earlier informal, zero-cost, in-conversation estimate of LLM performance) — this run's real result (74.1% accuracy, below the hand-crafted baseline) contradicted the earlier informal estimate (93.1%), which is documented as a methodological finding in Section 6.2 of the report.
- Verification, before finalizing model choice and pricing, of current Claude API model IDs and Claude Haiku 4.5 pricing directly against the maintained API-skill documentation, rather than from the assistant's own recollection.
- A report-writing pass: reconciling the existing draft against the assignment's official Required Sections list (extracted directly from the course's Final Project Guidelines page), identifying missing mandatory sections (Authors, Statement of Contributions, AI Prompts Used, formal References, Appendix, Discussion vs. existing solutions), and restructuring the report accordingly.

## Demo & Video Preparation

- "好了，现在看看demo视频怎么录。我现在运行email_priority_demo.html， 选用gmail 邮箱显示 Failed to fetch 。另外，demo直接不要中文版本了。最后给一个准确率随批次图像，总之你觉得怎么展示比较好就怎么来" (time to record the demo video; running email_priority_demo.html with the Gmail source selected shows "Failed to fetch"; also drop the Chinese version of the demo entirely; and give an accuracy-by-round chart at the end, whatever presentation you think works best) — led to: diagnosing the fetch failure as the FastAPI backend simply not running, translating `email_priority_demo.html` and its sample data fully to English (removing the language toggle), and confirming the existing per-round accuracy chart on the summary screen was already an adequate answer to the last request rather than building a new one.
- "等一下，还是failed to fetch，为什么，是认证过期了吗" (wait, still failed to fetch, why — is it an expired auth token?) — the first fix had started the backend through a browser-preview tool whose server only answered requests from that same sandboxed tool, not from the user's real browser; this follow-up prompted verifying with `lsof`/`curl` from a plain shell that nothing was actually listening on port 8000 on the real machine, and restarting the server directly via the ordinary shell instead.
- "再重启一下服务，又fetch失败1" (restart the service again, fetch failed again) — a later, identical recurrence after a Claude Code session restart killed the backgrounded server processes; resolved the same way, and documented here since it's a real, repeatable operational gap (background dev servers do not survive a session restart) rather than a one-off mistake.

## Report Formatting Review

- A detailed formatting-review prompt covering three specific issues: a duplicated "Accessed 2026, 2026" year in bibliography entries [6] and [11], a request to quote 1–2 actual prompts verbatim in the "AI Prompts Used" section rather than only summarizing and pointing at this log file, and a request to add a minimal Appendix section with a code-repository link and pointer to the README. Root-causing the reference-year bug required comparing the two broken `@misc` entries against three working ones in `references.bib` to isolate the actual difference (a `url` field vs. a `\url{}` macro embedded in `howpublished`).
- "看看文件夹里有哪些文件，哪些是需要push的哪些不用" (look at what files are in the folder, which need to be pushed and which don't) — this audit surfaced that the repository's install instructions lived in a file named `SETUP.md` rather than `README.md` (so it wouldn't auto-render on the GitHub repo page), that there was no documented way to reproduce the report's own figures/tables from `backend/gmail_eval/`, `backend/llm_eval/`, and `backend/enron/`, and that an entire saved Canvas course-page bundle (`Final Project Guidelines.html` and ~50 associated asset files, ~6.2MB) had been accidentally committed in the very first commit. Fixed by renaming `SETUP.md` to `README.md`, adding a reproduction-commands section (verified against each script's own docstring rather than guessed), and removing the stray Canvas files from version control.

## Presentation Preparation

- Iterative requests to draft a spoken presentation script against the course's official grading rubric (pasted in full), progressively cut down in scope across turns — from a 3-speaker ~5-minute script, to a single-narrator ~3-minute script handing off to a teammate's live demo — each revision re-checked against a word count (`wc -w`) to keep the estimated speaking time inside the assignment's 5±1 minute window instead of just guessing.
- Follow-up requests to draft a companion slide-deck as a reusable text prompt (rather than generating an actual .pptx) for pasting into an external slide tool — clarified first, via a direct question back to the user, whether an actual `.pptx` file or a text prompt was wanted, then iterated on slide count and information density per explicit follow-up instructions: "太长了，内容可以多但，页面要少" (too long — content can stay dense, but fewer pages) → consolidated 10 slides into 6 denser ones; "给个ppt prompt 要简洁" (make it concise) → cut back down to a terser 7-slide version; "prompt里多写点吧" (write more into the prompt) → initially misapplied to the slide prompt before the user clarified it meant this log file instead, corrected in the same turn.

## Grading Rubric Reconciliation

- "读一下我加入的requirement html文件，说说下一步应该做什么" (read the requirement HTML file I added, and say what to do next) — surfaced that a second, separately-saved Canvas page (`Report Requirement.html`, the assignment's actual attached grading rubric) described a Kaggle/notebook-style project structure (Introduction/Proposed Method/Related Work/Related Implementations/Data Analysis/Analysis/Experimental Setup/Results/Conclusion/References, with specific point values) that conflicted with the general course guidelines page used earlier. This was flagged explicitly as a conflict rather than silently resolved, since it materially changes the report's required structure.
- "确认过了，这份就是真实适用的，按它重新组织" (confirmed, this one is really applicable, reorganize according to it) — the user's own confirmation after presumably checking with the course; triggered a full restructuring of the report into the rubric's exact section order and point categories, including a real literature search (via web search, not invented citations) to reach the "5–6 related-work papers" and "2–3 Kaggle implementations" the rubric specifically asks for.

## LaTeX Conversion

- "有没有能把我的文稿变成论文格式的工具？" (is there a tool that can turn my draft into paper format?) followed by a specific two-column journal LaTeX template (JCSA) pasted in full — led to a full conversion of the Markdown report into that template, including building a `references.bib` file, adapting the template's cosmetic journal-submission metadata (fake ISSN/volume/received-revised-accepted dates) into neutral course-project framing rather than leaving misleading publication claims in place, and a static validation pass (brace balance, `\cite`/`\ref` cross-reference checks) in place of a full local compile, since installing a LaTeX distribution locally required a `sudo` password the assistant could not supply interactively.
- Follow-up compile-error triage from Overleaf output pasted back by the user: a fatal error from a literal full-width exclamation mark (U+FF01) in a formula (fixed by describing the character in prose instead of embedding the un-typesettable glyph, since the auto-suggested `inputenc` fix would not have supplied the missing font glyph), an unused `multicol` package warning (removed), and clarifying that several other warnings (bibtex-backend fallback, font-size substitution, "undefined references") were benign or a normal multi-pass compile artifact rather than actual bugs, so as not to over-fix things that were not broken.

## Data Re-Verification and Follow-Up Analysis

- "再跑一下sathvik的，他更新了数据" (re-run Sathvik's, he updated the data) — re-ran the personalization learning-curve script against the updated export, discovering a real +54pp lift, then investigating why: found the Skip class was concentrated in a single recurring sender (a digest email), which lets `sender_hist` memorize the pattern almost immediately — flagged as a caveat rather than reported as a clean win, paralleling an equivalent caveat already documented for the Enron exploration.
- "为什么sathvik的一开始personalized就90了，一开始不应该和coldstart一样吗" (why is Sathvik's personalized accuracy already ~90% at round 1 — shouldn't it start the same as cold-start?) — a sharp catch that the batch/round script's "round 1" already includes training on the first batch, not a true zero-feedback state; resolved by adding an explicit "Round 0" data point (untrained prior scored on the first batch, identical for both series by construction) to the figures and script, rather than just explaining the discrepancy away.
- "我发现vignesh的表现不好的原因是，虽然都是推送工作的邮件，有的标题是具体岗位他就会看，但如果是...这种抽象的他就会跳过" (I think I found why Vignesh's results are poor — for job-alert emails, he reads ones with a specific job title but skips generic ones) — verified directly against the raw labeled data (not taken on faith), which confirmed a cleaner and different pattern than proposed (role relevance, not title specificity — some specific-titled roles were also skipped), added to the report's Analysis section as the precise mechanistic explanation for that dataset's negative personalization lift.
- A request to relabel two teammates' datasets by hand after suspecting careless labeling — declined, since replacing real users' subjective Keep/Skip judgments with the assistant's own guesses would fabricate the empirical foundation the report's entire argument depends on; offered instead to audit the existing labels for internal consistency, which is how the sender-concentration and role-relevance findings above were actually reached.
- "算算Method Accuracy Precision Recall ... 二者的f1" (calculate the F1 for both methods) — computed and added an F1 column to the hand-crafted-vs-LLM comparison table, showing the hand-crafted model wins on F1 as well as accuracy, not only via a precision/recall trade-off.
- "那个方差的阴影面积可以不用加，效果不好" (the variance shading doesn't look good, drop it) and a request to regroup the three datasets' learning-curve panels (the two where personalization helped together, the one where it hurt on its own, with a zoomed per-panel $y$-axis) — both incorporated directly into the figure-generation script and regenerated from the real underlying data, not just re-styled after the fact.

## Content Trimming for Page Budget

- A request to cut the report to fit an 8-page budget, with explicit direction to prioritize cutting from restated/non-rubric-scored content (e.g., the "AI Prompts Used" section, which is mandatory but not an itemized rubric line item) over the highest-point-value "Analysis" section — resulted in roughly a 22% reduction in body word count across several editing passes, verified after each pass by re-counting words and re-checking brace balance and citation/label consistency, rather than cutting blind and hoping it compiled.
- This same review pass caught a stale factual claim left over from an earlier draft (the Introduction still stated that "one teammate's export contains zero Skip labels," which had been true before Sathvik's dataset was updated, per the entry above) and removed it while trimming, rather than leaving an outdated claim in the final text.

## Note on completeness

This log reflects the sessions available to the assistant compiling it. If any teammate used Claude or another AI tool in an untracked session (e.g., for the HTML frontend tools, the Gmail data-collection process, or slide/video preparation), those prompts should be added here before submission, since the grading requirement is to include **all** prompts used, not only those captured in this log.
