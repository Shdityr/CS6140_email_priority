fastapi==0.139.2
pydantic==2.13.4
pydantic_core==2.46.4
python-dotenv==1.2.2
starlette==1.3.1
uvicorn==0.51.0

# Training-only deps for backend/enron/train_prior.py — not needed by the
# live FastAPI service, which only reads the resulting enron_prior.json.
numpy==2.3.5
scikit-learn==1.7.2

# Eval-only dep for backend/llm_eval/claude_classify.py — not needed by the
# live FastAPI service.
anthropic==0.120.0
